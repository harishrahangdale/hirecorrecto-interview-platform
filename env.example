# Database
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/hirecorrecto

# Gemini API
GEMINI_API_KEY=your_gemini_api_key_here
# Model options: gemini-2.5-pro (best quality, recommended), gemini-1.5-pro (good quality), gemini-1.5-flash (faster/cheaper), gemini-2.0-flash-exp (experimental), gemini-pro (older)
# Note: If your selected model is not available, the system will automatically fallback to gemini-pro, then gemini-1.5-flash, then gemini-1.5-pro
GEMINI_MODEL=gemini-2.5-pro
USD_TO_INR_RATE=83.5

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# Storage
STORAGE_BACKEND=local
UPLOAD_DIR=./uploads

# Server
PORT=5004
NODE_ENV=development

# Frontend/Backend URLs (for separate deployment)
# CLIENT_URL: Frontend URL - Required for CORS and email links when deploying separately
#   - Development: http://localhost:5173
#   - Production (separate deployment): https://your-frontend.netlify.app
#   - Production (Docker Compose): http://localhost:5173 (or your domain)
CLIENT_URL=http://localhost:5173

# VITE_API_URL: Backend API URL - Required for frontend when deploying separately
#   - Development: Not needed (uses Vite proxy)
#   - Production (separate deployment): https://your-backend.onrender.com/api
#   - Production (Docker Compose): Not needed (nginx proxies /api)
#   Note: This is a frontend environment variable (VITE_ prefix required)
#   Set this in your frontend build environment (Netlify, Vercel, etc.)
# VITE_API_URL=http://localhost:5004/api

# JWT
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRES_IN=7d
